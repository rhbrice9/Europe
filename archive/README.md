# Legacy Version

`index-legacy-v1.html` is the original single-file HTML/React app that used
Claude's built-in `window.storage` API. It won't run outside of Claude.ai.

The current app (root of this repo) is a full Next.js + Supabase project
that deploys to Vercel and works for all 7 travelers simultaneously.
